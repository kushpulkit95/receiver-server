# Receiver Server

A lightweight TCP receiver used to test the Data Refinery Simulator.

## Features

- Listens for incoming TCP connections
- Receives CDR and NAT records
- Stores received records into a flat file
- Runs as an independent Java application

## Purpose

This project is a development/testing utility for the Data Refinery Simulator and is not part of the production simulator.